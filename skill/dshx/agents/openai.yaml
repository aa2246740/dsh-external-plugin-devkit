interface:
  display_name: "dshx"
  short_description: "Develop and activate external DSH plugins safely"
  default_prompt: "Use dshx to classify the plugin lifecycle branch, read the matching contract, and verify each activation layer without unnecessary Host restarts."
